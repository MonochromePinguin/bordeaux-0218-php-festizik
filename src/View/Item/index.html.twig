{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Items</h1>
    <ul>
        {% for item in items %}
            <li><a href="/item/{{ item.id }}">{{ item.id }}. {{ item.name }}</a></li>
        {% else %}
            <li>Aucune entrée présente</li>
        {% endfor %}
    </ul>
{% endblock %}
