{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Item {{ item.title }}</h1>
    <p>{{ item.id }}. {{ item.name }}</p>
    <a href="/item/edit/{{ item.id }}">Edit</a>
    <a href="/">Back</a>
{% endblock %}
